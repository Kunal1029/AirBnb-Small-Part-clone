<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
  
    <header class=" border-black border-2">
      <nav class="flex items-center justify-between  h-16 px-12 lg:gap-8">
        <a href="#">
          <i class="fa-regular fa-compass fa-2xl" style="color: #6d1717"></i>
        </a>

        <div
          class="dropdown-menu absolute -top-full left-0  py-2 flex flex-col w-full gap-6 items-center lg:static lg:flex-row lg:justify-between"
        >
        <ul class="flex flex-col lg:flex-row lg:gap-8 items-center gap-6">
          <li class="b">Home</li>
          <li>All Listings</li>
          <li>Add new Listings</li>
        </ul>

          <div class="lg:flex-row lg:gap-6 flex flex-col items-center gap-6">
            <button>Login</button>
            <button>Sign Up</button>
          </div>
        </div>

        <div class="toggle-button lg:hidden">
          <i class="fa-solid fa-bars fa-lg"></i>
        </div>
      </nav>
    </header>
  
  <!-- <h1 class="underline">All Listings</h1> -->
  <form action="/newData" method="GET">
    <button>Add new</button>
  </form>
  <ul>
    <% for(let listing of allData) { %>
    <li>
      <a href="/showdata/<%=listing.id%>"><%= listing.title%></a> &nbsp; &nbsp;

      <form action="/edit/<%=listing.id%>" method="GET">
        <button>Edit</button>
      </form>

      <form action="/delt/<%=listing._id%>?_method=DELETE" method="POST">
        <button>Delete</button>
      </form>
    </li>
    <%}%>
  </ul>

  <script src="scripts/script.js"></script>
</body>
</html>
